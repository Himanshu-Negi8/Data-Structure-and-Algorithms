#include<bits/stdc++.h>

using namespace std;

int main()
{
	vector<vector<string>>v;
	unordered_map<string,vector<string>> mp;
	
	for(string x:strs){
		auto word =x;
		sort(x.begin(),x.end());
		 mp[s].push_back(word);
		}
		
		for( auto a : mp){
			v.push_back(a.second);
		}
		
	}
	
}
